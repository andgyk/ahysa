{
  "name": "ahysa",
  "version": "1.0.0",
  "description": "bots bots bots bots bots bots bots",
  "main": "./dist/index.js",
  "scripts": {
    "test": "yarn lint && rimraf dist && yarn build && node .",
    "dev": "nodemon --exec yarn test",
    "build": "coffee -o ./dist -c ./src",
    "lint": "coffeelint src"
  },
  "repository": {
    "type": ".git",
    "url": "https://github.com/andgyk/ahysa.git"
  },
  "author": "Andgyk",
  "devDependencies": {
    "coffeelint": "^2.1.0",
    "coffeescript": "^2.7.0",
    "nodemon": "^2.0.16",
    "rimraf": "^3.0.2"
  },
  "license": "CC-BY-SA-4.0",
  "dependencies": {
    "comma-number": "^2.1.0",
    "dotenv": "^16.0.1",
    "eris": "^0.16.1",
    "mongoose": "^6.3.4",
    "parse-ms": "2.1.0"
  }
}
